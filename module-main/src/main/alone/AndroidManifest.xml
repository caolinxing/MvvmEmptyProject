<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhulong.main">

    <application
        android:name=".application.MainModuleApp"
        android:allowBackup="true"
        android:icon="@mipmap/base_ic_launcher"
        android:label="@string/main_name"
        android:roundIcon="@mipmap/base_ic_launcher_round"
        android:supportsRtl="true"
        xmlns:tools="http://schemas.android.com/tools"
        tools:replace="android:label"
        android:theme="@style/common_AppThemeStart">
        <activity android:name="com.zhulong.main.ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>