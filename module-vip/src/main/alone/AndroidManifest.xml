<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhulong.vip">
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".view.application.VipModuleApp"
        android:allowBackup="true"
        android:icon="@mipmap/base_ic_launcher"
        android:label="@string/vip_name"
        android:roundIcon="@mipmap/base_ic_launcher_round"
        android:supportsRtl="true"
        xmlns:tools="http://schemas.android.com/tools"
        tools:replace="android:label"
        android:theme="@style/common_AppThemeStart">
    </application>

</manifest>